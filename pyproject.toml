[project]
name = "db-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "duckdb>=1.5.5",
    "jupyter>=1.1.1",
    "nbconvert>=7.17.1",
    "pytz>=2026.3.post1",
]

[project.scripts]
db-project = "db_project:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
